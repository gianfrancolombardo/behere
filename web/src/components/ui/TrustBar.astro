---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---

<ul class="flex flex-wrap gap-2">
  {
    items.map((item) => (
      <li class="rounded-full border border-border bg-surface/60 px-3 py-1.5 text-xs text-text-muted">
        {item}
      </li>
    ))
  }
</ul>
